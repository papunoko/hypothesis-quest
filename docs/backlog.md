# バックログ

最終更新: 2026-09-20
フェーズ定義: [roadmap.md](roadmap.md) / 仕様: [concept.md](concept.md)

## 現在の優先順位（2026-09-20 夜・公開品質）

- F-01/F-02/F-03、H-08 は実装済み・ローカル検証済み。**未デプロイ**。詳細はHANDOFF冒頭と `docs/eval/journey-check-2026-09-20-quality.md`。
- 次: 案Bの文章を初見2人で試す → 採否判断。人の試遊が済むまで導入は差し替えない。
- J-01/J-02/F-05: 読み取りの残課題を評価セットへ。最新 `eval:lru` は11/14（途中案12/14もあるが、最終版の値ではない）。否定形、複合例外の順番、言い換えの弱い判定が残る。
- F-04/H-07: 観測のない体験主張・通常チャットとの比較。ボット成功を人の理解と扱わない。
- 以下の当日チェック欄には歴史的な構成名がある。現行保存先はD1、秘密値は `.dev.vars` / Workers Secrets、公開はWorkers。運用は deployment.md を正とする。

---

## S1 — 成立

- [x] **T-01** 題材の実装（`src/subject/orders.ts`・Idempotency-Key）
- [x] **T-02** 事例カード6枚を確定（`src/subject/cases.ts`）。実結果は `orders.test.ts` で実行して一致確認
- [x] **T-03** Next.js + TS 初期化（手動 scaffold。`npm run dev` / `npm test`）
- [x] **T-04** 画面1枚（仮説入力 / 事例比較 / 履歴 / 相棒の台詞枠 / 調べたこと）
- [x] **T-06** 仮説の履歴表示
- [x] **T-10** `/api/predict`。Jev キーはサーバー側（`.env.local` の `JEV_API_KEY`）
- [x] **T-11** Noul 4軸で仮説文の条件を読み、コードで事例に当てはめる
- [x] **T-12** 4問を1リクエストで評価。実結果は渡さない
- [x] **T-13** 照合（予想 vs 実結果）をコード側で
- [x] **T-14** 選択: 反証 → 決まらない → 未提示 → 終了
- [x] **T-15** 相棒の定型台詞（事例ごと）
- [x] **T-17** Jev 失敗時フォールバック（エラー表示＋固定順で次を開く）
- [x] **T-20** 「決まらない」を第二優先で提示
- [x] **T-21** 終了時に「正しい」と言わない
- [x] **T-30/32** 低 confidence（< 0.55）は「解釈の確認」表示
- [x] **T-50/51** 調べたこと一覧・対象範囲の明示（ヘッダー）
- [x] **T-53** 段階ヒント3段
- [x] **T-56** Cloudflare Tunnel を前提に公開手順を文書化（`docs/deployment.md`）。Wrangler は不使用
- [x] **T-16 検証** 商品→C3、依頼ID→C4を実Jevで確認。再測定は `npm run eval:jev`
- [x] **T-54** 注文APIの動作確認台本（[demo-orders.md](demo-orders.md)）。本番題材はT-54'で別途作成
- [x] **T-55** 注文API向け冒頭30秒の語り（同上）

## J — Jev 適合性

実測（2026-09-20 夕方、`npm run eval:jev`）: 英語の問い＋true/false 基準文の設計Bで、典型6文は軸 0.9 台／無関係文は 0.0 台で読めた。応答 200〜640ms。
**未保証なのは、この6文以外の言い回しと、題材2の軸。** 方向性の議論は [HANDOFF.md](HANDOFF.md)「Jev 適合性の方向性」。

### 評価基盤（すべての前提）

- [ ] **J-01** 評価セットを作る（`scripts/eval-set.json`、20〜30文）。軸ごとの正解ラベル付き
      → 含める: 典型6文の言い換え / 否定（「商品が違っても登録する」）/ OR / 例外（「ただし〜なら」）/ 英語 / 誤字 / 雑談 / **題材2の階段5段**
- [ ] **J-02** `eval:jev` を評価セット対応に（軸ごとの正誤・混同・平均応答時間。同じ文を3回投げて分散も見る）
- [ ] **J-08** 閾値 `NAMED=0.6 / UNCLEAR=0.4` を評価セットの分布から決め直す

### ライブ読み取り（入力中に4軸の棒が動く）

- [x] **J-03** `/api/read` を分離（読み取りだけ返す。題材2は6主張軸＋種別/話題）
- [x] **J-04** 入力欄の下の鏡。400ms・4文字以上・Abort＋世代番号。表示閾値0.35、未言及軸を隠す
- [x] **J-05** IME 対応。変換中は送らず、確定後に読み取る（Playwright回帰あり）
- [ ] **J-06** 文の途中は棒を薄く、送信で確定した読みを濃く。ヒステリシスで棒のちらつきを抑える
- [ ] **J-07** これが「解釈の確認」の主経路になる。事例側の「解釈の確認」表示は補助に格下げ（P3 を別機能として作らない）
- [ ] **J-13** 1セッションあたりの想定リクエスト数を見積もり、レート制限とコストを確認（2026-09-20 深夜: `/api/support` が加わり、待機中は最大 5 回/分。READ_LIMITER 側に寄せた）

### 読み取り設計の比較

- [ ] **J-09** 設計C（含意判定）を並走実装（`src/lib/jev-entail.ts`）。事例ごとに帰結文3本を固定データで書き、Noul「仮説からこの帰結は導けるか」を 18問/1リクエスト
- [ ] **J-10** 評価セットで B と C を比較。「決まらない」の検出精度も見る（C は沈黙と否定の区別が弱い想定）→ **D-05** で採用を決める
- [ ] **J-11** 否定・OR・例外の扱いを決める（B に軸を足すか、C に寄せるか）。J-10 の結果待ち
- [ ] **J-12** 設計D（典型文への Choice 分類）を「あなたの仮説を、こう読みました: 『…』」の表示に使う。予測の主経路にはしない

### 試遊で見ること

- [ ] **J-14** 棒を見ながら書くと、言い回しを Jev に寄せて「自分の言葉」が薄れないか。concept §11 の観察項目に追加

## U — ウミガメ化（質問も仮説も受け、累積する）

2026-09-20 夜の決定: 遊びの形をウミガメのスープに寄せる。出題者の答えは検証済みの真相からコードが出す。Jev は入力がどの軸の話か・質問か主張かを読むだけ。
設計の骨子は [concept.md §12](concept.md)。

### 入力の受け方

2026-09-20実装: 質問／主張Choice・質問の話題Choiceと、題材2専用の比較データを追加。U-03は専用データのみ実装（共通化は未完）。U-05は本人が種別を選ぶフォールバックまでで、LLM分割は未実装。回答する問いを画面に明示し、未検証の問いは「まだ答えられません」と区別する。

- [x] **U-01** 種別判定: Choice {質問, 主張, どちらでもない} を `/api/read` に足し、鏡の先頭に「質問として読んでいます／仮説として読んでいます」を表示
- [x] **U-02** 質問への答え: はい／いいえ／場合による／関係ない。軸ごとの**最小対**（その軸だけが違う事例の対）から機械的に出す。「場合による」は対を2枚並べる（L5/L6）
- [ ] **U-03** 題材データに軸ごとの最小対を持たせる（T-60 の共通スキーマに1列追加）
- [x] **U-04** 主張への答えは従来どおり（反証カード／決まらない／一致）
- [ ] **U-05** 質問と主張が混ざった入力: Jev の {質問, 主張} が両方とも中間帯（例 0.35〜0.65）のときだけ LLM に渡し、**質問1文＋主張1文に書き直させる**。書き直した各文を Jev に読ませ直す。LLM は判定しない、分割するだけ。JSON を守らない応答は捨てて「どちらとして読めばいい？」の2ボタンに落とす

### 核心への近さ（採点にしない）

- [ ] **U-30** 「説明が通るテスト」: 最新の仮説が通る事例数を、帳面の横に **枚数ではなく段階**（まだ／半分／あと少し／全部）で出す。検証済み事実から計算。Jev を使わない
- [x] **U-31** 「核心に触れた」: 軸ごとに `core: true/false` を題材データに持ち、質問がその軸に触れたら相棒が「この質問は真相の中心に触れている」と一言。ウミガメの出題者の「いい質問です」に相当。個人の理解ではなく質問の性質への反応
- [~] **U-32**（2026-09-20 深夜、ユーザー指示で一部実装）: 鏡の見出しに「核心度 0〜1」を数値で出した（types / singleFast の最大値、0文字なら0）。3段表示・ヒステリシスは未実装。答えの方向を漏らす懸念は残るので試遊で見る。元案: 鏡の温度（任意）: 最新の読み取りベクトルと真相ベクトルの近さを「遠い／近づいた／かなり近い」の3段で。数値は出さない。ヒステリシスで揺れを抑える。**答えの方向を漏らす**ので、デフォルトはオフにして試遊で比べる
- [x] **U-33** 出さないもの: 軸の総数、理解度%、正答率。concept §2「明確に避けるもの」と同じ線

### 帳面（累積記憶・SQLite）

実装注: 保存対象は回答を得た送信入力（下書き・読み取り失敗・種別確認待ちは未保存）。帳面本体は外部送信しないが、入力文のJev送信は従来どおり。セッションCookieで分離、再読込で復元。DBは自動削除しない暫定運用で、D-08の保持・削除方針は残す。

- [x] **U-10** `node:sqlite`（Node 26 組み込み）でローカル保存。`better-sqlite3` は使わない（Windows でのネイティブビルド回避）
- [x] **U-11** 1入力1行: セッションID、入力文、種別、読み取りベクトル、出した答え、見せた事例、時刻。外部送信なし。README に「入力はローカル SQLite に保存」を明記
- [x] **U-12** 帳面パネル: 「調べたこと」を帳面から描く。確定事項（はい／いいえ）と「まだ聞いていない軸」を分けて出す。軸名は聞いた軸だけ表示（未入力の軸から答えを漏らさない）
- [ ] **U-13** 矛盾の指摘（相棒・生成なし）: 新しい仮説の読み取りが、帳面の確定事項と食い違うときに一言出す。「さっき順番は関係あると確認したが、いまの仮説は順番に触れていない」
- [ ] **U-14** 帳面から評価セットを拾う導線: 実プレイヤーの言い回しを J-01 に流す（正解ラベルは手で付ける）

### クリアと途中提出

3002版でU-20〜22を実装。提出は仮説・判断・コメントを明示送信してSQLite保存。再読込後も回答を見直せる。モデルは実測で日本語が安定したgemma4:31bを採用（GLM案から変更）。根拠不足・個人採点をJevで検問し、0.30以上／障害は定型回答へ。U-30の段階表示は提出後に実装済み、帳面横への常設は未実装。U-24は通常回答の原因非開示と、明示的な「理由も知りたい」ヒントまで実装（ヒント3段階化は未完）。

- [x] **U-20** クリア条件はコードで検査: 最後に提出した仮説が7事例を通る。理解の採点はしない。併記するのは「まだ聞いていない軸」の有無（事実）
- [x] **U-21** 途中提出の解説を LLM で生成（GLM 5.3 flash 想定）。入力は帳面と検証済み事実のみ。出力に「生成」ラベル。コードについて新しい事実を言わせない
- [x] **U-22** 語りの検問（decisions.md C案）: 生成した解説を Jev Noul で「渡した事実にない主張を含むか」を検査し、含むなら出さず定型文に落とす
- [ ] **U-24** 「場合による」の割れ方の説明を LLM に書かせる。渡すのは最小対2枚の操作列・実結果・**割れている軸名と違う特徴**。ただし出す段階を分ける: プレイ中は2枚を並べて「違いを1つ探して」の定型文のみ（違う特徴は渡さない＝漏れようがない）。ヒント3段目と提出後の解説では違う特徴を渡して説明させる。生成文は Jev Noul「渡していない特徴に言及しているか」で検問（`probe-voice-leak.mjs` の延長）
- [x] **U-40** 助け舟（2026-09-20 深夜）: 常時表示の「何を書けばいいかわからない」をやめ、止まり方（経過秒・文字数・スクロール・ポインタ・進み具合）を `/api/support` で Jev に読ませて種別 start/stuck/explore/send を決める。入力文は送らない。文言と候補はコード固定（`support-card.tsx`）。閉じた種別は同じ事例では再表示しない。本人が開く「ヒント」も残す。判定記録 `eval/support-probe-2026-09-20.md`。**人の試遊で「出るタイミングが早い／遅い」を見るまで閾値（15秒・paused 0.6・explore 60秒）は仮**
- [x] **U-41** 鏡の見出しを「種別チップ＋核心度」に（2026-09-20 深夜）。「こう読んでいます」「質問として読んでいます」の文言は廃止。軸の棒は折りたたみ「読み取りの内訳」へ
- [x] **U-42** 「観察を区切ってレビューを書く」「提出したレビューと回答を見る」をボタンに（2026-09-20 深夜）
- [ ] **U-43** 助け舟の候補文（`STARTERS`）に companion-does-not-leak を当てる。現在は `hint` キーでないため規則の対象外。型・書き方・順番の3軸と質問形のみで、1個だけの特例と typed は入れていない
- [ ] **U-23** 入力中の資料提示: いま触れている軸の図・`_make_key` の該当行だけを Choice で選んで出す。触れていない軸の参考仮説はヒント3段の側に置く（答え漏れ防止）

### 決めること

- [x] **D-07** 「場合による」は**最小対が1つでも割れたら必ず出す**（閾値なし）。割れた対を2枚並べる。理由: 山場（L5/L6）を機械が丸めないため（2026-09-20 決定）
- [ ] **D-08** 帳面の保持期間とセッションの切り方（ブラウザごと／URL ごと）
- [x] **D-09** LLM 解説の失敗時（タイムアウト・検問不合格）の定型文

## S3 — 本番題材（lru_cache）への載せ替え

2026-09-20実装更新: 題材2を `/` に載せ、導入・7事例＋L8・ライブ読み取り・L5/L6比較・レビュー下書きまで動作。実装の詳しい範囲と評価の残課題は [HANDOFF.md](HANDOFF.md) 冒頭。全面的な共通化とB/C比較は引き続き未完。

題材1（注文API）は開発用ダミー。デモは [subjects/lru-cache.md](subjects/lru-cache.md) で行う（2026-09-20 決定）。
**前提: J-10（読み取り設計の採用）が先。** 採用した設計で題材2の軸または帰結文を書く。

- [ ] **T-60** 題材の共通スキーマを切る（世界の説明 / 軸 / 事例: 表示用の操作列・特徴ベクトル・実結果 / 未探索論点 / 戻り先）。`cases.ts` は注文API専用なので一般化する
- [x] **T-61** 題材2の6主張軸（rule/form/order/types/singleFast/typed）を実装。5概念軸の実験とは区別する。一般精度はJ-01/J-02で継続評価
- [x] **T-62** L8は別論点として自由に開く。仮説の反証選択には入れない
- [x] **T-63** `scripts/verify-lru.py` と `npm run verify:lru`。WSL実行と画面8件の照合をREADMEに記載
- [ ] **T-64** 画面の対象表示・相棒の台詞・ヒント3段・戻り先（`_make_key` の行）を題材2用に書く
- [x] **T-65** `/` を題材2に、`/orders` に開発用ダミーを保持
- [ ] **T-54'** デモ台本は題材2で組み直す。山場は L5/L6 の逆転

## H — ハーネス（体験と題材を批判的に測る）

2026-09-20 整備。使い方は [harness.md](harness.md)、結果は `docs/eval/`。原則: 測ることはスクリプト、読んで決めることはスキル。

- [x] **H-01** 固定戦略ボット `npm run harness:stress`（早漏者・詰め込み屋・列挙者・理解者・報告者）。実 Jev、LLM なし、docs/eval/stress-*.md
- [x] **H-02** 帳面レポート `npm run harness:notebook`（D1 ローカル＋旧 SQLite → 核心接近曲線・離脱点）
- [x] **H-03** 題材検査 `npm run subject:check`（真相が全事例を通る / 報告者の期待が最小対を割る / 矛盾が原因を言わない / 矛盾として読める / 質問が出る）
- [x] **H-04** 題材候補の収集と順位付け `npm run subject:mine -- --repo owner/name` → `npm run subject:rank -- --in ...`（GitHub の not planned イシュー → Jev 4命題）
- [x] **H-05** jev-lint 導入。`rules/` に4本（1問1判断 / LLM は判定しない / 相棒の台詞が漏らさない / 体験主張に観測がある）＋同梱の commit ルール。fixtures と baseline 付き
- [x] **H-06** スキル `subject-forge`・`journey-check`。外部スキル `jev-lint`・`game-design-reality-check`・`stress-testing-game-concepts` を導入
- [ ] **H-07** codex 対照群の手順書（同じ PR を普通のチャットで解く群と、終了後に一文で規則を言えるかを比べる）。採点は既存の軸読みで
- [x] **H-08** 題材2専用 `LRU_HOLDOUT` H1を追加。クリア後だけ3択、選択後に実測、非保存・非送信。CPython実行＋Playwright確認済み。共通スキーマはT-60、人の転移測定は未実施
- [ ] **H-09** jev-question-single-condition が `src/lib/jev.ts` の注文API質問を拾えない（instructions と criteria が別の Record に分かれていて subject に文が無い）。注文APIはダミーなので、直すなら文をインラインにする
- [ ] **H-10** jev-lint 0.3.2 の Windows 問題2件を上流へ: ast-grep の起動（ラッパーで回避済み。PATH の winget 版を優先）、block ルールの expect.yml ラベルがパス区切りで突き合わない（calibrate --labels で代替）

### ハーネスが出した findings（2026-09-20）

ユーザー決定（2026-09-20 夜）: **F-01/F-02 → F-03 → H-08 の順で着手する。** 方針と検証手順は [HANDOFF.md](HANDOFF.md) 冒頭「次にやること」。F-04 は台帳として残し、F-05 は F-01 の後に再測定してから判断。

- [x] **F-01** singleFast≥0.6かつtypes<0.6なら一般の型条件をfalseとして関連軸から外す。生の読みは維持。明示的な一般型条件・他軸の曖昧さを消さない回帰あり。実Jevで真相7/7、理解者クリアを確認
- [x] **F-02** typesの問いを「異なる型を別呼び出しとする一般則の主張か」に変更し、同一視する文をfalse基準に追加。実Jevで報告者はL5 mismatch/L6 match。任意の否定文を解決したわけではない
- [x] **F-03** L2/L4/L7を比較への問いに修正。対象ファイルはjev-lint 3パスで0 findings。全体リントの残件は別途台帳に保持
- [ ] **F-04** 体験主張に観測がない節: journey.md 7節、concept.md 5節（`npm run lint:jev:check` の claim-has-evidence）。試遊で観測するか、仮定と明記するかを節ごとに決める
- [ ] **F-05** 列挙者（質問の答えを繋いだ文）は 5/7 で止まる。繋ぎ文が真でないか（順番の扱い）、読み取りの問題かを切り分ける
      → 修正後6/7。最終文はorderがnamedにならずL4未確定。順番を区別すると明言しているので読み取り側の残課題。一般のOR/否定・合成の保証はない。
- [ ] **F-06** 修正後の詰め込み屋は3手で7/7、理解者は5手で7/7。前者の最終文には正しい単一特例も含まれるので、単なる軸名の列挙による不正通過とは断定しない。H-07/H-08の人の説明・転移で比較する（ボットを落とすためにクリア条件を変更しない）

### 文脈分離AI試遊の追加findings（未修正）

証拠: [試遊2役＋独立監査](eval/blind-2026-09-20/report.md)。人の理解・楽しさ・モバイル視認性の証拠ではない。全体DOM読解と既有知識の限界を含む。

- [ ] **F-07** H1を確かめた後も「まだ確かめていないこと」に同じ例が残る。A最終画像で表示不整合を確認。H1の表示済み状態と一覧の表現を整合させる（保存仕様を勝手に拡張しない）
- [ ] **F-08** 質問の対象/極性の読み替えと答え・帳面の対応を追試。Aは「当たる？」→別命題への「はい」、Bはfloat→bool→int→float比較。局所的な期待不一致であり、誤回答の認定は元質問/回答対象/返答を照合してから
- [ ] **F-09** 鏡が「単一のintなど」を「int・strが1個だけの特例」に具体化。A画像と入力原文で表示差を確認。本人の文にない情報を補う範囲を検証し、鏡由来の語を独力発見と数えない
- [ ] **F-10** レビュー生成文の語彙/関連性: Bの本文に `maintainerResponse` が露出。性能/互換性の検証依頼に対し内部条件の説明不足を返すA/Bの期待不一致も確認。判定への混入は認定せず、表示文と提出意図の対応を評価する

## 落としたもの（当日）

- T-05 固定順ダミー（Jev 直結にしたので不要。フォールバックとしてのみ残存）
- T-07 人に見せる検証（時間切れ。デモ本番が代わりになる）
- T-22 曖昧仮説のチューニング
- T-31 「いや、違う」の再評価導線（いまはフォーカス移動のみ）
- T-33 confidence 表示レビュー（「確信度」と表示。理解率とは書いていない）
- T-40〜42 根拠パネル（`codeRef` の文字列表示のみ）
- T-52 終了画面の「成果物側の不明点」（未確認論点のみ）

## 決めたこと

- **D-01** confidence 閾値 = 0.55（`page.tsx` の `CONFIDENCE_THRESHOLD`）
- **D-02** Jev エラー時: エラー文を表示し、「予想なしで次の事例を開く」ボタンを出す
- **D-03** 実結果の検証記録 = `src/subject/orders.test.ts`（`npm test` で6/6）
- **D-04** 題材コードは AI（Claude）が書いた。Python 案は環境になく TS に変更
- [ ] **D-05** 読み取り設計の採用（B 軸Noul / C 含意判定 / 併用）。J-10 の結果で決める
- [x] **D-06** ライブ読み取り: 400ms、4文字以上、IME中は停止。中断＋世代番号で古い応答を捨てる

## 検証ログ

[HANDOFF.md](HANDOFF.md) 冒頭に再開後の実測を記録。Noulの質問と判定基準を調整してT-16を確認。任意の仮説の一般精度は未保証。

## 監視したいリスク

| リスク | 兆候 | 対応 |
| --- | --- | --- |
| Jev が「決まらない」に逃げる | 全事例 undetermined | 初回プロンプトで発生 → 「唯一のルールとして適用」に書き直して対応。再発時は criteria の文言を調整 |
| 6文以外で読めない | 評価セット（J-01）で軸の正答が落ちる | 基準文の反例を足す。改善しなければ設計C（J-09）へ |
| ライブ読み取りが試験に見える | 棒を「点数」と受け取る | 棒のラベルを「こう読んでいます」にし、数値を出さない |
| 採点ゲームに見える | 試遊者が「正解は？」と聞く | 相棒の台詞と終了画面の文言を直す |
| 「で？」と言われる | 冒頭30秒で痛みが伝わらない | T-55 を先に書いて人に聞かせる |
