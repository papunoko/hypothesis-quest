# journey-check 2026-09-21 final round

入力: `stress-2026-09-20T13-49-46-479Z.md`、`notebook-2026-09-20.md`、`subject-check-lru-cache-2026-09-20T13-49-50-285Z.md`、全体lintの体験主張13候補、前回 `journey-check-2026-09-21-support-ux.md`。ボット・題材の規則は今回変更せず、最新の既存記録を用いた。

## 判定（1行）と最強の理由

**Test-ready（人の試遊用として）**。終盤で再現した表示不整合を修正し、実生成の失敗文も固定回帰にした。ただし操作・照合が通ることと、理解・転移が起きることは別。人への効果はまだ支持されたとは言えない。

## 欠陥（記録で示せる）

### 修正前のブラウザ試遊

2026-09-21 00:43〜00:44 JST、Codex内ブラウザ、`127.0.0.1:8787`。localhostの利用者Cookieには触れず、別ホスト名の空の帳面から開始。視覚スクリーンショットとAXツリーで操作。モデルは題材の正解を既知であり、初見・人間の学習試験ではない。

1. `typed=Trueだとどう変わる？` → 質問として読み取り、L6/L7、「はい」。質問用の支援ボタンから送信した。核心度0.9（正誤ではない）。
2. 「理由も知りたい」を開いてからレビューへ。ヒント本文は今回独立に品質判定していない。
3. 最後の仮説: `引数の並びをそのまま鍵にする。ただし int か str が1個だけなら、その値自体が鍵になる。typed=Trueでは型も区別する`。判断は追加検証、コメントは「typedの比較で差があることを確認しました。近道を外すなら、変更後のメモリ使用量と性能も検証してください。これは動作確認用のレビューです。」
4. 3事例を見た状態で7事例の照合が一致。生成文は次の通り（この試遊で得た自作アプリの出力を保存）。

> typedの比較による挙動の差については、L6とL7の実測結果から確認できています。
>
> 一方で、仮説にある「intかstrが1個だけなら、その値自体が鍵になる」という点については、L1やL5などの読み取り結果から推測はされますが、それが具体的にどのような仕組みで動作し、どのような影響を与えるかという説明に穴があります。
>
> また、変更後のメモリ使用量と性能については、allowedFactsに測定結果が含まれていないため、現時点では未検証の状態です。次は、仮説にある「1個だけの場合の処理」の正当性と、懸念されている性能への影響を具体的に確かめる必要があります。

5. H1で「もう一度計算する」を選択。実測は記憶。開いた後も未確認一覧に同じ問いが残り、見出しも「見ていない事例」のまま（AXとスクリーンショットで確認）。

**F-07**: 既読状態の表示不整合。予想を選んだらページ内で既読にし、未確認一覧から除外。探索へ戻る/提出切替後も維持。再読込でリセット。保存・送信・7事例のクリア条件は変更しない。見出しは「最後にひとつ、別の事例で試す」。

**F-10（一部）**: 内部名 `allowedFacts` の露出。生成指示はクリアでも一律に「説明に残る穴」を要求していた。既知の内部フィールド名5種はコードの確定的な形式チェックで定型文へ戻す。解説は照合と未測定の性能を分離し、根拠中のコードを未確認とする矛盾は既存Jev検問の基準へ明記。生成文は判定に流さない。過去に保存した文章の書換え・再検問は行っていない。

### 検証中に見つけた通常回答の漏れ

最初の `npm run eval:narration` は終了コード0だったが、質問「型は関係ある？」への生成文は以下で、既存の検問を通った。

> 結果は場合によります。今回の実測では、引数が1つの時はもう一度計算し、2つの時は記憶した結果を返すという異なる挙動が確認されています。2つのテスト結果を比較して確認してください。

これは通常回答で発見させたい差を名指ししている。**生成が通ったことだけを成功条件にしていた評価の不足**。通常回答は事例IDと実結果に限定し、実測の言い換えであっても比較の着眼点を名指すのはspoilerと基準へ明記。閾値・質問数は変更しない。失敗文そのものと漏れない対照を実APIで各2回検問する回帰を追加した。全ての言い換えを防ぐ保証ではない。

### 修正後の観測

- 00:59:17 JST、同じ隔離ホストで再提出。実Jev照合は全部、実LLM回答は「用意した7事例において、説明と実測は一致しました。一方で、変更後のメモリ使用量と性能については、今回の実測範囲外となります。」。
- H1の「記憶」を選ぶと実測が表示され、未確認一覧から該当文が消えた。探索へ戻っても消えたまま、質問欄が再び操作可能。終盤の配置をスクリーンショットで確認。
- `review-round-2026-09-20T15-53-14-080Z.json`: レビュー検問3文×2回＋生成2回、8/8。
- `review-round-2026-09-20T15-58-43-293Z.json`: 最終版。レビュー/質問の固定検問10件＋生成4件、14/14。生成文は本文も読んだ。2回が同文となる場合もあり、独立した幅広い評価ではない。
- `jev-lint-2026-09-21-final-round.json`: 対象4ファイル41 subjects、31 cached、0 findings。通常回答の最後の補強前の記録なので、補強後のnarration単体記録を別途残す。ルール自体は未変更。
- `jev-lint-2026-09-21-final-narration.json`: 補強後のnarration、8 subjects（5 cached）、0 findings、未判定なし。いずれもdry-run後にラッパー経由。意味検問の正しさの保証ではない。
- 単体40/40、TypeScript、レビュー画面回帰5/5、Workerビルド、WorkerのPlaywright30/30（UI25＋実API5、2.4分）通過。最後の通常回答補強後も再ビルド・単体40/40・実API5/5（49.4秒）。UIはこの最後の補強では未変更。本番確認は公開後に追記する。

TypeSafeの手順で一問一判断、既知の形式はコード、意味の検問はNoulを維持。[Noul](https://docs.typesafe.ai/primitives/noul)・[State](https://docs.typesafe.ai/concepts/state)・[API](https://docs.typesafe.ai/api)の通常ページを参照した（index/Markdown URLは取得失敗）。

## 情報不足（記録が無い）

- 因果の鎖「謎→質問→最小対→気づき→一文→レビュー」のうち、入力→実測・提出の操作は観測。**気づき→自分の一文・転移**は既知の正解を使ったため不明。
- 詰め込み屋3手7/7は以前のstressで観測済みだが、正しい特例も書いており理解なしの支配方策とはまだ言えない。軸語彙を並べるだけで通るかは別途評価が必要。理解者の到達不能は最新の固定文では再現しない。
- 行動を変える状態は、L5とL6の結果が割れる状況と、7事例一致でも変更後性能は未測定という状況。ただしプレイヤーが実際に判断を変える証拠はない。
- F-10の未知の語彙・レビュー意図への関連性は未解決。核心度やヒントが答えを探す遊びを強めるかも不明。通常チャット対照（H-07）なし。

## 試遊が必要（人でしか分からない）

導入案Bの初見2人試験は未実施のまま。画面上のボタンに到達できることはブラウザ試験で測れるが、自発的な質問・言い直し・終了後の説明は測れていない。台本を渡さず、最初の質問までの時間、L5/L6で何に気づくか、終わった後の一文を取る。

## 次の判別試験（1つ）

既存案Bの文章だけを初見2人へ提示する最小試験を維持。2分以内に2人とも挙動を確かめる具体的な質問を自発的に1つ出せたら画面への採用候補とする。片方でも操作説明や「何を書けばよいか」が必要なら、その箇所を記録して導入を再検討する。今回の操作テストをその代わりにしない。
