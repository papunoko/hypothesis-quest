# journey-check 2026-09-21 — 修正→試遊→修正

入力: stress-2026-09-20T13-49-46-479Z.md、notebook-2026-09-20.md、subject-check-lru-cache-2026-09-20T13-49-50-285Z.md、jev-lint-2026-09-20-full-quality.json（既存体験主張13候補）、9/20〜21のCodex内ブラウザ操作。HEAD `3f714d7` からの修正。以下は人間の初見試遊ではない。

## 判定（1行）と最強の理由

Test-ready。観測された操作上の矛盾は局所修正できたが、入力→気づき→理解/転移という中心の体験主張は未検証。

## 欠陥（記録で示せる）

修正前のトレース（9/20、8787、画面を視認。会話のブラウザ出力が原記録）:

1. 導入下部で開始→事例上部が画面外。レビュー移行でも画面位置が残った（F-14）。
2. ヒント→別候補「書き方まで同じなら記憶を返す」→L2「この仮説ではまだ決まらない」（F-13）。
3. 「引数の型が違っても、値が同じなら記憶を使えますか？」→「論点の中心に触れています」と同時に核心度0.2（F-12）。
4. 同じ入力を送信済みのまま回答を読んでいると「この一文で試してみますか？」。質問なのに支援ボタンは「この仮説で…」（F-11）。

1回目の修正→再試遊（9/21、3002）:

- 書き方候補を「書き方は関係ある？」に変更。実ブラウザで「はい」＋L2/L3＋検問通過の生成回答に到達。
- 核心度を表示専用Noulへ分離。初案は短い「型は関係ある？」0.62/0.64、「引数の個数で変わる？」0.49/0.46。問いに題材文脈が足りず、固定8文×2は12/16。失敗記録: `support-ux-2026-09-20T14-59-42-906Z.json`。
- 短い入力もlru_cacheの文脈で読む基準に修正し、16/16（`support-ux-2026-09-20T15-00-25-179Z.json`）。同じ肯定/否定で高くなることを確認。初期候補はL2 mismatch、3つの質問候補は比較を返す。閾値を通りやすくする変更はしなかった。
- ブラウザでは型の長い質問は核心度1.0（小数1桁表示）、L5/L6と「場合による」、中心への言及。回答を読む間に自動催促は戻らなかった。時間制御の回帰では送信後90秒の問い合わせ増加なし、編集後は支援が復帰。

2回目の修正:

- スマホ回帰で見出しのviewport比0を検出。スクロール対象がスマホで `display: contents` だった。実体のある見出しへ移して390px/1440pxの開始→レビュー→再開を通した。
- 実ブラウザで右側の入力列内だけスクロールが残ったため、結果切替で列内もtop=0へ。型の回答・レビューの画面で上部が見えることを再確認。
- 質問用助け舟の本文も質問用に変更。遅い支援応答を編集/次事例/終了それぞれ独立に回帰する。
- liveテスト1本が削除済みの「質問として読んでいます」を参照して失敗。現行の種別チップに合わせ再実行して通過。別の並行実行ではPlaywright出力ディレクトリが競合したため、最終確認は1プロセスで行う。

追加のユーザー指摘（レビュー後は質問できない？／Cookieクリア導線）への修正:

- 旧実装は `ended` で入力を無効化し、明示的な再開が「もう一周」だけ。新しい「質問・観察を続ける」は確認済み事例・帳面を戻さず、レビューを非表示で保持することで下書き・判断・受領回答を残す。初見で発見できるかは未評価。
- 「新しいセッションを始める」はCookieだけ更新。確認文には旧履歴へのアクセス喪失・未提出文の喪失・DB非削除・他タブへの影響を列挙。UIリセット失敗のテストはNextのroute announcerを含めたstrict locatorが原因で1回失敗し、対象をsession-controlsへ限定した。
- 核心度の不正/欠落は表示のみ未確定にする。比較6軸の不正は引き続き拒否する単体テストを追加した。
- Codex内ブラウザで保存済みの0:09:44の回答を開き、追記中の下書きを用意→続行→「順番は関係ある？」→L4/「はい」/検問通過生成文→レビューへ戻る、を実操作。下書きと0:09:44の回答、帳面5件を維持した。Cookie切替は隔離したPlaywrightセッションで検証し、利用中の内蔵ブラウザのCookieは切り替えていない。

## 情報不足（記録が無い）

Concept id: hypothesis-quest-lru。操作=質問/仮説/提出、状態=事例/読み取り/帳面、進行=7事例照合。既存ボットで真相7/7、詰め込み屋3手7/7は維持された記録だが、今回新しい人の試遊はない。詰め込みが通ることは理解不要の支配方策の証明ではない。通常チャットとの対照もない。

今回の操作は既有知識あり。前回のlocalhost Cookieを継続し、過去の帳面を復元した。ヒント候補を自分で発見した知識とは数えない。生成文・確率は1セッション/固定文の結果で、一般精度ではない。TypeSafe公式文書はこの回のweb取得では到達できなかったため、導入済みskillと既存v1実装を使用した。

## 試遊が必要（人でしか分からない）

- 核心度を正答率と誤解しないか、数字を上げるための入力に寄らないか（U-32）。
- 助け舟の時間閾値は適切か、質問候補によって自発的な問いを奪わないか。
- 核心度の初案の失敗は文脈不足、スマホの失敗はUI実装の問題。人の楽しさの判定には転用しない。

## 次の判別試験（1つ）

初見の人2名に現行版で、説明を足さずに最初の質問→比較→レビューまで試してもらう。核心度が何の数値か本人に説明してもらい、正答率/理解度と答えたら表示を再検討。これは表示判断の小試験で、理解効果を一般化しない。

## 最終検証

- 単体 `npm test`: **39/39**。TypeScript / `git diff --check` 通過。
- `npm run build:worker`: 通過。WindowsのOpenNext警告はあるが、生成Workerの動作まで確認。
- ローカルWorker（8787）のPlaywright: **30/30**（UI25、実Jev/LLM5、2.3分）。再質問・下書き/提出回答保持、Cookie確認/取消/失敗・Origin拒否・他Cookie維持・切替後の帳面/提出が空・旧セッションのD1記録が残ることを含む。開発版でも追加のsession2件/live2件は通過。
- 対象6ファイルのjev-lint: **26 subjects / 0 findings**、18 cached、5 requests。`jev-lint-2026-09-21-session.json`。全体の旧13体験主張などは今回解消したとは扱わない。先行するsupport-only記録も保持。
- 3002（Next dev）と8787（最新ローカルWorker）を起動して引渡し。ブラウザ実操作は3002。公開中のWorker/DNS/期限/Secretsは変更せず、**本番デプロイ・コミットなし**。
- journey-checkに従い、失敗→修正の記録と人による初見試遊の不足を分離した。UI回帰・固定文の通過は理解効果の実証ではない。
