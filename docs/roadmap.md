# ロードマップ

最終更新: 2026-09-20 夜（ハッカソン終了。本番は https://ebiharadev.org で公開中、期限 2026-10-01 00:00 JST）
企画: [concept.md](concept.md) / タスク: [backlog.md](backlog.md) / 測る道具: [harness.md](harness.md)

**原則: 各フェーズの終わりに必ず「触れるもの」がある。** 公開後は「触れるもの」に加えて、**測った記録（docs/eval/）がある**。

## いまのフェーズ — R 公開品質（2026-09-20 夜〜09-30）

ハッカソンは終わり、公開にあたってやり残したことを全部やる段階。順番はユーザー決定。

1. **F-01 / F-02** 真相の一文がクリアしない（`types` 軸 0.36〜0.39 → L6 が読み取り未確定）。公開ゲームとして最重要。方針は [HANDOFF.md](HANDOFF.md) 冒頭
2. **F-03** 相棒の台詞 L2 / L4 / L7 の漏れを疑問形に
3. **H-08** 未提示事例 `f(1.0)` → `f(True)` でクリア後を締める
4. 案 B（謎を先に置く導入、[journey.md §0b](journey.md)）の最小試験（2人・文章だけ）→ 採るなら §0 置き換え
5. F-04 体験主張の台帳（journey §10）を試遊で観測に変える／H-07 codex 対照群
6. 運用側（他セッション）: 保持期間・削除導線・レート制限の見直し、期限後の撤去手順

各項目の完了条件: 直したら `npm run harness:stress`・`npm run subject:check`・`npm run lint:jev:check` を回し、`docs/eval/` に日付付きで残す。デプロイは [deployment.md](deployment.md) の手順で。

---

## 当日に決めたこと（grill の結果）

| 論点 | 決定 |
| --- | --- |
| 時間 | 当日、残り約1.5h。P0+P1 を1本に潰す |
| 結果軸 | 件数だけでは C4/C5 が反証にならない → **4択**（登録される / 同じ結果 / 拒否 / 決まらない） |
| 仮説の向き | 「どう判定するか」ではなく **「何を守っているか」** を書かせる。C5 は「再送では増えない」への反証になる |
| デモ形式 | **発表者が2分プレイ**。仮説は台本で固定できる |
| 題材コード | TS で書き `node --test` で6事例を検証（Python はストア版スタブのみで未インストールだったため） |
| confidence 閾値 | 0.55 未満は反証と断定せず「解釈の確認」へ |
| 題材の位置づけ | **題材1（注文API）は開発用ダミー。本番題材は題材2（lru_cache）** → [subjects/lru-cache.md](subjects/lru-cache.md) |
| Jev 読み取り | 設計A（事例ごと Choice）は状況文に引っ張られ不採用。設計B（仮説文だけを Noul 軸で読む）を英語の問い＋基準文にして典型6文は読めた。一般精度は評価セットで測る（J） |
| 解釈の確認 | 事例側の確認表示ではなく、**入力中のライブ読み取り**を主経路にする |
| 遊びの形 | **ウミガメのスープ**。質問も仮説も受け、SQLite の帳面に累積。答えはコードが最小対から出す。途中提出の解説だけ LLM（GLM 5.3 flash 想定）が語り直し、Jev が検問 → [concept.md §12](concept.md) / backlog U |
| 公開（夜） | Cloudflare Workers + D1、ebiharadev.org、期限 10-01 00:00 JST に 410。IP 別レート制限。LLM は gemma4:31b（GLM は JSON 指定を無視したため） → [deployment.md](deployment.md) |
| 測る道具（夜） | 固定戦略ボット・帳面レポート・題材検査・jev-lint 4本・スキル2本。初回で F-01〜F-05 が出た → [harness.md](harness.md) / backlog H・F |
| 導入の再検討（夜） | 「一文で書け」は宿題で謎ではない。矛盾の2行を先に置く案 B を**仮定**として追加、人で未試遊 → [journey.md §0b](journey.md) |

---

## 全体像

```
S1 成立      ── 画面1枚 + /api/predict + 6事例 + 照合 + 選択            済
S2 締まり    ── 終了画面・未確認の可視化・フォールバック・台本            済
J  Jev適合性  ── ライブ読み取り 済 / 評価セット・設計B/C の比較 未
U  ウミガメ化 ── 質問・帳面(D1)・最小対で答える・提出解説はLLM+Jev検問   済（U-05 分割・U-13 矛盾指摘 未）
S3 本番題材  ── lru_cache を / に載せ替え                                 済（共通スキーマ T-60 未）
P  公開      ── Workers + D1 / ebiharadev.org / 期限ゲート / レート制限   済（撤去手順 未）
H  ハーネス  ── ボット・帳面レポート・題材検査・jev-lint・スキル          済（初回結果あり）
──────────── ハッカソン終了（2026-09-20）
R  公開品質  ── F-01/02 → F-03 → H-08 → 案B の試験 → F-04/H-07           ★いま
S4 根拠・解釈訂正 ── コード行への実リンク／「いや、違う」で Jev の読みを直せる（T-31 は F-01 の代替手段でもある）
```

---

## S1 — 成立（実装済み）

**到達点**: 自分が書いた仮説によって、次に出る事例が変わる。

- `src/subject/orders.ts` 題材（AIが書いた想定の注文API）/ `orders.test.ts` 6事例の検証記録
- `src/subject/cases.ts` 事例カード6枚 + 未確認の論点
- `src/lib/jev.ts` Jev 呼び出し（仮説文のみ。Noul 4軸を1リクエストで読む）
- `src/lib/select.ts` 照合と選択（反証 → 決まらない → 未提示 → 終了）
- `src/app/api/predict/route.ts` APIルート1本
- `src/app/page.tsx` 画面1枚

**完了条件**: 違う仮説を2つ入れて別の事例が出る。→ 実測は [backlog.md](backlog.md) の検証ログ参照。

## S2 — 締まり（実装済みの範囲）

- 終了時「用意した6事例を見終えました」＋未確認の論点（表示済みと仮説の一致を区別）
- 「調べたこと」一覧（✓ 提示済み / ─ 未提示 / ─ 未確認）
- Jev 失敗時は「予想なしで次の事例を開く」に落ちる（エラーは表示する）
- 低 confidence は「解釈の確認」として表示（採点しない）
- 段階ヒント3段
- Cloudflare Tunnel で公開する手順（Quick Tunnel / 固定URL）
- 注文APIの動作確認台本（[demo-orders.md](demo-orders.md)）。本番題材向け台本はT-54'に残る

---

## J — Jev 適合性

**到達点**: 任意の言い回しの仮説を、どの程度読めるかが数字で言える。入力中に読み取りが見える。

現状（2026-09-20 夜）: 設計B（英語の問い＋基準文の Noul 6軸）で本番題材の主要な階段は読める（`eval:lru` 11/14）。ライブ読み取りは実装済み（400ms・IME・Abort・表示閾値 0.35）。一般精度は未測定のまま。ハーネスが F-01（真相の文で `types` が 0.36〜0.39）と F-02（否定形の型主張が中間帯）を出しており、**評価セット（J-01）の代わりに、当面は `harness:stress` と `subject:check` の固定文で回帰を取る**。帳面（U-14）が評価セットの供給源になる。

1. **評価セット**（20〜30文、軸ごとの正解ラベル）と `eval:jev` の拡張。題材2の階段も入れる
2. **ライブ読み取り** — 入力停止 300〜400ms で4軸の棒が動く。IME 変換中は投げない。これが「解釈の確認」の主経路になる
3. **設計C（含意判定）を並走**し、評価セットで B と比較 → 採用を決める（D-05）

**完了条件**: 評価セットで採用設計の軸正答率が出ている。ライブ読み取りが動く。
順番の理由: ライブ読み取りは B/C どちらでも棒に出すのは読み取りベクトルなので、比較の結果を待たずに作れる。

## S3 — 本番題材への載せ替え（実装済み）

**到達点**: `functools.lru_cache` の事例8枚で一巡が回り、L5/L6 の逆転から `_make_key` の1行へ戻れる。→ `/` が題材2、`/orders` にダミー。L8 は別論点。T-60（共通スキーマ）と T-64（台詞・ヒント）は未完で、T-64 は F-03 として R で扱う。

## P — 公開（実施済み）

Workers + D1、`ebiharadev.org/*` の Route、期限ゲート（`custom-worker.ts`）、IP 別レート制限。詳細と撤去の注意は [deployment.md](deployment.md)。

## H — ハーネス（整備済み）

固定戦略ボット・帳面レポート・題材検査・jev-lint（`rules/` 4本）・スキル `subject-forge` / `journey-check`。初回結果と限界は [harness.md](harness.md) §4〜5。次に足すのは H-07（codex 対照群）と H-08（holdout、R で実施）。

## S4 — ハッカソン後（R の後）

- 事例カードの `codeRef` を実ファイル・行へのリンクにする（いまは文字列）
- 「いや、違う」で Jev の読み取りを訂正して再評価する導線
- 曖昧な仮説での「決まらない」の出方をチューニング（現状は出にくい方向に寄せてある）

## 将来（切った案の乗せ先）

| 案 | どこに乗るか |
| --- | --- |
| **B 解釈が見える入力欄** | S4 の拡張。打ちながら確率が揺れる |
| **A Three Clueリンター** | 題材を作る側の道具 |
| **C 語りの検問所** | 相棒を生成LLMにしたとき必須 |
| **D ドキュメント整合linter** | 題材の自動生成に進んだとき |
| 題材の差し替え | OSS の実イシューから事例を拾う案（当日案として出たが未着手） |

詳細と不採用理由: [decisions.md](decisions.md)
